<h1>Buses</h1>

<table border = "2">
  <tr>
    <th>ID</th>
    <th>Bus Name</th>
    <th>Bus Type</th>
    <th>Bus Brand</th>
    <th>Bus Capacity</th>
    <th>Details</th>
  </tr>

  <% @buses.each do |bus| %>
    <tr>
      <td><%= bus.id %></td>
      <td><%= bus.name %></td>
      <td><%= bus.typed %></td>
      <td><%= bus.brand %></td>
      <td><%= bus.capacity %></td>
      <td><%= link_to "view", bus %></td>
    </tr>
  <% end %>
</table>
<br>
<%= link_to "Create New Bus", new_bus_path %>

